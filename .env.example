ADMIN_PASSWORD=admin

# Vercel Blob (required for admin menu/promotions edits on Vercel)
# Create a Blob store in the Vercel project → Storage → Blob, then link it.
# Vercel injects BLOB_READ_WRITE_TOKEN automatically in production.
# For local blob testing: vercel env pull
BLOB_READ_WRITE_TOKEN=
